import React from 'react'
import Styles from './styles.module.css';

export const GradientBoxes = ({ className }) => {
  return (
    <div className={`${Styles.container} ${className}`}></div>
  )
}

